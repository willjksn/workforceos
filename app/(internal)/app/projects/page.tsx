import { PhasePlaceholder } from "../_components/ui";

export default function ProjectsPlaceholderPage() {
  return (
    <PhasePlaceholder
      title="Projects"
      description="Delivery project execution is reserved for a later phase."
    />
  );
}
