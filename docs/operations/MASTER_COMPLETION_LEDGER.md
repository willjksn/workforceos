# Master completion ledger

Date: 2026-09-08  
Approved by: user (master remaining-work roadmap)  
Status: this file is the remaining-work system of record going forward. The old audit-only backlog is not.

Do not delete completed rows. Do not mark COMPLETE without evidence (commit SHA, test output, URL, query result). Use Military Talent Partner in new prose. Do not merge parked research at `1a356d3` / `follow-on/parked-working-tree`. Do not start Phase 11 research runtime. Title ≠ Access is locked (DEC-AUTH-002). Production API keys are Phase M.

Statuses: `NOT STARTED` · `IN REVIEW` · `APPROVED` · `IN BUILD` · `BUILT` · `TESTED` · `COMPLETE` · `BLOCKED` · `DEFERRED`

Canonical chain: Decision Log → Requirements / Master Spec → WorkforceOS → `sites/pierone` if public → Scout knowledge → playbooks / manual → Academy → Changelog.

Company operating model (how PierOne runs the five offers): `docs/business/PIERONE_OPERATING_MANUAL.md`. Repeatable delivery maps: `docs/business/SERVICE_PLAYBOOKS.md`. Engineering deploy/recover/PITR stays in `docs/operations/WORKFORCEOS_OPERATING_PLAYBOOK.md`.

| Phase | Work Item | Status | Dependency | Owner / Role | Source of Truth | Required Decision | Exit Criteria | Evidence | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A | A1. Delete four launch-smoke PDFs from production R2 | COMPLETE | Operator-confirmed production R2 delete | Ops / Strategy & Technology Administrator | Production `files` rows; Sep 7 launch cleanup | None | Only those four objects deleted; bucket not wiped | Operator-confirmed 2026-09-09: the four launch-smoke PDF objects were removed in Cloudflare R2. Archived `files` rows remain archived. This environment did not re-delete and did not wipe the bucket. Keys (historical): `applications/00000000-0000-4000-8000-000000000001/c144d9f8-b0e3-4886-8d8a-65a7fabaecc3/1788818236625-pierone-production-smoke-test.pdf`; `military-talent/00000000-0000-4000-8000-000000000001/cf6726d9-747a-49e2-9454-e58e11a73988/1788818238718-pierone-production-smoke-test.pdf`; `applications/00000000-0000-4000-8000-000000000001/10f1aad2-1f17-47de-904f-a32b1bb07f71/1788833208788-pierone-production-r2-smoke.pdf`; `military-talent/00000000-0000-4000-8000-000000000001/13512d17-2cd5-4221-a1ab-e09da2d5df34/1788833211155-pierone-production-r2-smoke.pdf`. | COMPLETE on operator confirmation. Did not re-delete. Did not list or wipe the bucket. Archived DB rows left archived. |
| A | A2. Confirm Neon behind Vercel production is not fixture-filled | COMPLETE | Production `NEON_BRANCH` / `DATABASE_URL` | Ops / Strategy & Technology Administrator | Vercel production env; production SQL | Do not delete unless confirmed fixture and not real client/candidate data | Live production target named and queried; Harbor / Taylor Ellis / Cedar Ridge / Navy EM seed IDs absent or explicitly triaged | Vercel production `NEON_BRANCH=production-launch` (`vercel env pull --environment production`, 2026-09-08). Queried the launch production database (`ep-spring-scene-axohlt4c-pooler`, same DB that holds the four archived smoke files). Harbor / Taylor Ellis / Cedar Ridge / Navy EM seed IDs: 0 rows. Labeled SkillBridge candidate count: 0. Active companies: 0. Active candidates: 0. Archived candidates: 4 (launch smoke names). Org row: `WorkforceOS` / `workforceos`. Local `.env.local` is `NEON_BRANCH=development` (`ep-rapid-rain-axls26v9-pooler`) and was not used for this query. | Playbook still warns that a Neon branch *named* `production` may hold fixtures. Live Vercel production points at `production-launch`, which does not. Did not run `db:seed:dev` or `db:seed:prod`. Did not delete archived smoke people/inquiries (already archived; not Harbor fixtures). Vercel `DATABASE_URL` value is sensitive and was not printed; decrypt returned no string to compare endpoint IDs. |
| A | A3. Correct stale post-launch backlog (domain + R2) | COMPLETE | Live hosts and storage adapter | Product Build | `docs/requirements/POST_LAUNCH_BACKLOG.md` | None | Domain and R2 rows marked complete; Checkr / calendar / QB / DocuSign left open | `https://app.pieronepartners.com` live. Vercel production `STORAGE_PROVIDER=s3` with R2 endpoint. Backlog rows in `590c480`. | Did not invent other completions. |
| A | A4. Clerk invite vs public sign-up | COMPLETE | DEC-SEC-001 | Strategy & Technology Administrator + Product Build | Clerk production instance `ins_3J19hL4sLCY5dcVWW4VuYh4g08m`; `lib/admin/invite-user.ts`; `lib/auth/sync-user.ts` | Invite-controlled (already accepted) | Public sign-up not a live exposure; uninvited Clerk users do not become local active users | Clerk production `sign_up_mode` was `public`; patched to `restricted` 2026-09-08 (`clerk config patch`, verified pull). Code in `590c480`. Invite path: Admin → People (`inviteOrganizationUser` + Clerk invitation to `/sign-up`). `syncLocalUser` no longer inserts unknown Clerk users. `db:bootstrap-admin` records a missing email as invited. | Existing Clerk-linked users still match by `clerk_user_id` or invited email. First-admin no longer depends on public sign-up. |
| A | A5. Production / release smoke after `30f0dec` | COMPLETE | Local development DB (`NEON_BRANCH=development`); do not write smoke onto production | Product Build | `npm run test:smoke`; `scripts/test-smoke.ts` | Fix only a clear Phase A P0 regression | PASS or recorded FAIL | `npm run test:smoke` PASS 2026-09-08 (exit 0) on `590c480` parent work. SMOKE 1–12; “Smoke tests passed.” `npx vitest run tests/admin-invite.test.ts`: 8 passed. No scripted regression vs `30f0dec+`. | Scripted smoke seeds foundation and writes a `smoke.ping` audit on the *local* development branch, not `production-launch`. |
| A | Create this ledger as remaining-work SoR | COMPLETE | User approval 2026-09-08 | Product Build | This file | Approved | File exists; playbook / AGENTS / registry point here | `590c480` on `main` (2026-09-08). Pointers: playbook, `AGENTS.md`, `REQUIREMENTS_REGISTRY.md`. | First artifact of the approved roadmap. |
| B | Confirm production AI is keyed or honestly labeled heuristic | IN REVIEW | Phase A | Strategy & Technology Administrator | `lib/ai/provider.ts`; Vercel `AI_API_KEY` / `AI_PROVIDER` | Capability-class model names (`AI_MODEL_FAST` / `STANDARD` / `REASONING`) vs single `AI_MODEL` | Production Scout/AI is not silently heuristic; health says which | `76f1828`. `vercel env ls production` 2026-09-08: `AI_API_KEY` and `AI_PROVIDER` exist as Encrypted on Production (and Preview). `vercel env pull` of production into a temp file returned empty values for both (length 0); file deleted; values never printed. Same decrypt gap as Phase A R2. System Health now labels LIVE vs HEURISTIC so production will show which once a decryptable key is present. | B1 audit: providers = `internal_heuristic` + OpenAI-compatible (`AI_BASE_URL`, default `https://api.openai.com/v1`). `OPENAI_API_KEY` aliases `AI_API_KEY`. Features using AI: agent runner (Scout tasks, matching, military mapping drafts, workforce analysis, proposal drafting, research recommend); resume parse and military translator are local heuristics; embeddings are development-hash `vector(1536)` (DEC-SEM-001, dimension unchanged). Missing key → honest `internal_heuristic` / `heuristic-v1`. No `gpt-*` hard-codes in app runtime. Production live vs heuristic: **unconfirmed** from this environment. |
| B | Map capability models onto the AI provider | COMPLETE | Provider abstraction | Product Build | `lib/ai/capabilities.ts`; DEC-AI-011 | Adopt FAST / STANDARD / REASONING env names | Documented mapping; no new provider brand in the UI | `76f1828`. `npx vitest run tests/phase-b-ai.test.ts tests/env.test.ts tests/phase9-scout.test.ts tests/phase7-ai.test.ts` — 32 passed (2026-09-08). DEC-AI-011. Env: `AI_MODEL_FAST` / `STANDARD` / `REASONING` / `EMBEDDING` with `OPENAI_*` / `AI_MODEL` aliases. | Keep the existing AI abstraction. Neon Gateway only if already pointed at via `AI_BASE_URL`. |
| B | Scout production validation (closed commands, no SQL, PII strip) | COMPLETE | Phase A; `candidate_pii.read` | Product Build | Scout command registry; `DEC-AI-001` | None | Closed commands only; never generate SQL; email/phone/compensation/resume stripped from model context | `76f1828`. Same vitest run 2026-09-08 (32 passed) plus `tests/phase9-scout.test.ts`. Search/summarize for companies, contacts, opportunities, candidates, jobs, Military Talent, employer opportunities, projects, finance, public content, knowledge/training. | Scout send stays hard-denied (`isScoutExternalSendEnabled() === false`). Heuristic fallback covered without live OpenAI. |
| B | AI spend, health, and review-queue operating path | COMPLETE | `agents.manage` already gates costs | Ops Admin / Managing Partner | `/app/ai-operations`; usage events | None | Spend/health visible to AI admins; humans approve material output | `76f1828`. Usage events record provider/model/taskType/tokens/cost/capability class; runs record user, latency (startedAt/completedAt), fallback (`retryCount`). Cost-limit alerts added. `/app/admin/system-health` distinguishes live vs heuristic. Review queue unchanged (`DEC-AI-008`). Recruiter `agents.manage` remains false (`tests/phase-b-ai.test.ts`). | Recruiters must not see cost admin (`agents.manage`). |
| C | Title ≠ Access in People UI | TESTED | Existing `user_roles` junction | Product Build + Managing Partner | `users.organizational_title`; `/app/admin/users` and `/app/admin/users/[id]` | DEC-AUTH-002 | Display title stored; access bundles are not job titles | Schema + People list/detail; `npx vitest run tests/phase-c-access.test.ts tests/rbac.test.ts tests/phase3-permissions.test.ts` (47 passed with related files). Production 2026-09-08: `organizational_title` column present. | Title is never an authorization input. |
| C | Assign multiple access bundles + permission overrides | TESTED | Title ≠ Access decision | Product Build | `user_roles`; `user_permission_overrides`; `loadPrincipalByUserId` | Deny-wins after bundle union (accepted) | N bundles in UI; effective permissions visible; templates copyable | Same vitest run; People detail checkboxes, overrides, copy, reset. Production 2026-09-08: `user_permission_overrides` exists. | One user can hold two bundles; server-side effective permissions apply deny after union. |
| C | Military Talent Partner access template (with Phase D rename) | TESTED | Phase C model | Product Build | Role slug `military-talent-partner` | Rename slug (accepted) | Template named Military Talent Partner | Migration `0013_nervous_maggott` + `sync-role-permissions` on production 2026-09-08: slug `military-talent-partner` present; specialist absent; recruiter `opportunities.read` count 0; intake owner Partner. | One-release alias `military-talent-specialist` → `military-talent-partner`. |
| D | Rename remaining Military Talent Specialist → Military Talent Partner | TESTED | DEC-MIL-005; role rename landed with C | Product Build | Decision Log, spec, emails, Scout, training leftovers | None for slug | No new Specialist prose in remaining docs/emails/Scout/training | Slug/seed/inquiry default moved with C. Remaining Specialist strings are the one-release alias (`lib/rbac/permissions.ts`), historical drizzle `0011`/`0012` snapshots, and this ledger. `npx vitest run tests/rbac.test.ts tests/phase9-scout.test.ts tests/nav.test.ts tests/phase3-permissions.test.ts tests/skillbridge-drafts.test.ts` — 34 passed; `npm --prefix sites/pierone test` — 12 passed; `npx tsc --noEmit` clean (2026-09-09). Commit `8269561`. | Do not start full Academy (Phase E). |
| M | Production API key runbook + inventory | TESTED | Phase B health labels | Ops / Strategy & Technology Administrator | `lib/env.ts`; `docs/operations/PRODUCTION_API_KEYS.md` | None | Every env var listed without values; production names inventoried | Runbook + `vercel env ls production` 2026-09-08 (Encrypted vs missing recorded in the runbook) | Actual secret values BLOCKED until present/decryptable in Vercel. Did not overwrite Encrypted keys. |
| M | Configure remaining production secrets | BLOCKED | Operator secret store + Vercel | Ops / Strategy & Technology Administrator | Vercel production env; System Health | Do not rotate live Encrypted keys without a defect | Required groups present; optional Phase I groups honestly unlabeled | Production Encrypted names listed in PRODUCTION_API_KEYS.md. Missing optional groups: calendar, Checkr, DocuSign, QuickBooks, SeekOut/Apollo, BLS/Census, `DATABASE_URL_UNPOOLED`, capability-class model names. `AI_API_KEY` name exists Encrypted; live vs heuristic still unconfirmed (decrypt empty). | Do not dump values. Do not put secrets in `NEXT_PUBLIC_*`. |
| D | Sync military intermediary model across docs / app / site / Scout / training | TESTED | DEC-MIL-005 | Product Build | Decision Log, requirements, public site, Pathway ops | None for copy; mapping rules stay versioned | Same model everywhere; no invented MOS maps; no duplicate candidates | Public + Pathway copy already shipped (`0f3fa1e` and later). Phase D finished leftover spec/email/Scout/knowledge/report/public CTA drift. Canonical flow locked in catalog, workflows, spec, Military overview, and seeded `military-talent-intermediary` knowledge. Employer brief remains reviewable prose (`tests/skillbridge-drafts.test.ts`). Same vitest/public-site/tsc run as the Specialist row (2026-09-09). Commit `8269561`. | Do not invent military-mapping rules. SkillBridge people stay Talent Network candidates. Academy (E) not started. |
| E | In-app Academy (help/training by effective access) | TESTED | Phase C + D | Product Build | New requirement (not parked research runtime) | Academy is not a full LMS | Training by effective access; completion does not auto-escalate permissions | `/app/academy` and `/app/academy/[slug]`. Required modules from `lib/academy/training.ts` (effective permissions, never title). Schema `user_training_progress` (`drizzle/0014_rainy_doomsday.sql`). Contextual `? Help` on Command Center, Companies, Opportunities, Discovery, Proposals, Jobs, Talent, Military, Projects, Finance, Admin People, Scout. Scout Academy links `/app/academy/[slug]`. `npx vitest run tests/phase-e-academy.test.ts tests/nav.test.ts tests/phase9-scout.test.ts` — 25 passed (2026-09-09). `npx tsc --noEmit` clean. Scout send still denied (`isScoutExternalSendEnabled() === false`). Commit `aa0122c`. Production Neon `production-launch` 2026-09-09: `0014` applied; `user_training_progress` present (`information_schema`). Checkpoint branch `prod-migrate-checkpoint-20260909` expires 2026-09-16. | Not a full LMS. Not Phase H. Completion does not grant permissions. |
| F | PierOne company operating manual (SOLVE → BUILD → OPERATE → screens) | COMPLETE | Phase D language lock | Managing Partner + Product Build | `docs/business/PIERONE_OPERATING_MANUAL.md` | None | Approved manual mapped to live screens; engineering playbook untouched | `docs/business/PIERONE_OPERATING_MANUAL.md` on `main` (`eb87590`, 2026-09-09). Knowledge seed slug `pierone-operating-manual` in `db/seed/phase7.ts`. | Display name locked later as Professional & Technical Search (DEC-SVC-005); slug remains `professional-search`. Phase G playbooks cite this manual. |
| G | Ten service playbooks mapped to screen / data / owner / approval / Scout | TESTED | Phase F; workflow DB records | Service owners | `docs/workflows/SERVICE_WORKFLOWS.md`; `services` / `service_workflows` tables; `docs/business/SERVICE_PLAYBOOKS.md` | Five launch services only | Each playbook cites a workflow record, not invented steps; knowledge slugs seeded; screens match live routes | `docs/business/SERVICE_PLAYBOOKS.md` + `docs/business/playbooks/` (10 files) on `main` (`1d63cd6`, 2026-09-09). Knowledge slugs in `db/seed/phase7.ts`. `npx vitest run tests/phase-g-playbooks.test.ts` — 5 passed. `npm run test:phase8` passed (engineering playbook untouched). | Playbook 6 is pathway ops, not a sixth offer. Academy (E) not started. Scout send still denied. |
| H | Employee onboarding Day 1–Week 4 + training states | TESTED | Phase C + E | Operations Administrator | Distinct from Phase 10 hire `/app/onboarding` | No auto-permission bump | Path exists; training states do not grant access | `796716b` on `main` (2026-09-09). `/app/academy/onboarding` and `/app/admin/users/[id]/onboarding`. Schema `staff_onboarding`, `staff_onboarding_equipment`, `staff_policy_acknowledgements`, `users.manager_id` (`drizzle/0015_dizzy_wrecker.sql`). Invite creates cadence Day 1. Week 4 review and Academy completion do not grant permissions. Recruiter still lacks `opportunities.read`. `npx vitest run tests/phase-h-onboarding.test.ts tests/phase-e-academy.test.ts tests/nav.test.ts tests/phase9-scout.test.ts tests/phase-c-access.test.ts` — 41 passed (2026-09-09). `npx tsc --noEmit` clean. Scout send still denied. ATS `/app/onboarding` unchanged. Production Neon `production-launch` 2026-09-09: `0015` applied; `staff_onboarding`, `staff_onboarding_equipment`, `staff_policy_acknowledgements`, `users.manager_id` present. | PierOne employee access+training, not candidate onboarding. |
| I | Live calendar OAuth scheduling | COMPLETE | Vendor app + Integration Hub tokens | Ops | Integration Hub; `getCalendarProvider()` | Named provider ready | Live Microsoft/Google events; mock labeled until then | `090ecf7`. Code path: `lib/calendar/live.ts`. `liveScheduling` true only with `GOOGLE_REFRESH_TOKEN` or `MICROSOFT_REFRESH_TOKEN` plus app client. Unconfigured = MOCK (`npx vitest run tests/phase-i-integrations.test.ts` — 13 passed, 2026-09-09). Production BLOCKED: missing `GOOGLE_*` / `MICROSOFT_*` Encrypted vars. | Production keys remain Phase M. |
| I | Human-approved email send (`scout.external_actions`) | COMPLETE | Phase I + confirmation | Product Build | DEC-AI-003; DEC-HIRE-004 | Explicit start | Send hidden until approved; humans send | `090ecf7`. Gates: `scout.external_actions` + `transactional_email.send` + confirmation token + Resend. Recruiters still lack `scout.external_actions`. Ops/admin/managing-partner hold the send permission. Same vitest run. | Resend Encrypted on Production; Scout send still cannot fire without human confirmation. |
| I | Live DocuSign envelopes | COMPLETE | DocuSign + webhook secret | Ops | Integration Hub | Named vendor ready | Manual execution remains until configured | `090ecf7`. Code path live when `DOCUSIGN_SECRET_KEY` + `DOCUSIGN_ACCOUNT_ID` exist. `applyDocuSignStatus` still requires `confirmed=true`. Production BLOCKED: missing `DOCUSIGN_*`. | Never mark executed without confirmation. |
| I | Live QuickBooks OAuth / AR post | COMPLETE | QB app + idempotency | Ops / Finance | Integration Hub | Named vendor ready | Posting enabled with idempotency | `090ecf7`. `postInvoice` idempotent on `quickbooks:invoice:{id}`. LIVE needs `QUICKBOOKS_REFRESH_TOKEN` + `QUICKBOOKS_REALM_ID`. Production BLOCKED: missing `QUICKBOOKS_*`. | Operating finance vs QB ledger unchanged (DEC-FIN-002). |
| I | Live Checkr HTTP API | COMPLETE | Checkr sandbox + webhook + counsel | Ops + counsel | `docs/integrations/CHECKR.md` | Counsel + vendor | Provider-hosted invitations | `090ecf7`. Live invitations when `CHECKR_API_KEY` set. Signed webhook + rate limit. Never auto-reject. No FCRA letters generated. Production BLOCKED: missing `CHECKR_API_KEY` / `CHECKR_WEBHOOK_SECRET`. | Counsel-sensitive notices stay honest. |
| I | Live SeekOut / LinkedIn / Apollo after internal search | COMPLETE | Integration Hub; WFOS-TAL-002 | Product Build | Talent Network first | Named provider ready | External sourcing only after internal search | `090ecf7`. SeekOut/Apollo live after internal search when API keys exist. LinkedIn still no scrape. Apollo still review-gated. Production BLOCKED: missing `SEEKOUT_API_KEY` / `APOLLO_API_KEY`. | Do not duplicate candidates. |
| I | Official Sentry SDK | COMPLETE | `SENTRY_DSN` | Ops | `@sentry/node` via `lib/observability/sentry.ts` | None | Official SDK when DSN present; redact secrets/PII | `090ecf7`. `SENTRY_DSN` Encrypted on Production. Official SDK init in `instrumentation.ts`. Thin DSN poster removed. No secrets/PII in events. | Source maps still optional; DSN not in `NEXT_PUBLIC_*`. |
| J | Command Center operating rhythms (leadership / ops / talent / military / finance) | TESTED | Real pipeline data; commercial spine | Managing Partner | Command Center | Cadence objects vs docs-only | Weekly rhythms visible in the OS | Command Center tabs at `/app?cadence=` leadership, operations, talent, military, finance (not extra nav items). Live SQL counts + 8-row exception lists in `lib/reporting/operating-rhythms.ts`. Recruiter still lacks `opportunities.read`; commercial widgets `weekly-pipeline` / `open-proposals` hidden. Scout `SHOW_DASHBOARD` weekly review is RBAC-before-model, no SQL, no candidate email/phone/compensation/resume. Academy `weekly-operating-review`. Manual leadership-cadence pointer. `eae6566` on `main` (2026-09-09). `npx vitest run tests/phase-j-rhythms.test.ts tests/phase-e-academy.test.ts tests/phase-h-onboarding.test.ts tests/phase9-scout.test.ts tests/nav.test.ts` — 42 passed. `npx tsc --noEmit` clean. Scout send unchanged. Phase K GTM board added as a sibling cadence. | Cadence boards are permission-gated review groups on Command Center. SQL rewrite remains Phase L. |
| K | 90-day GTM (Tier 1/2, Southeast BD, KPIs in the OS) | TESTED | Phase J; commercial spine | Managing Partner + Talent Partner | Opportunities / proposals already built | KPI set | GTM plan lives in WorkforceOS | `43eb0c2` on `main` (2026-09-09). Written plan `docs/business/PIERONE_90_DAY_GTM_PLAN.md`. Companies `gtm_tier` / `gtm_region` (`drizzle/0016_lyrical_giant_girl.sql`). Command Center `/app?cadence=gtm`. Recruiter still lacks `opportunities.read`; all GTM widgets `commercial`. Scout `SHOW_DASHBOARD` dashboard `gtm`. Academy `ninety-day-gtm-review`. Knowledge seed `pierone-90-day-gtm-plan`. `npx vitest run tests/phase-k-gtm.test.ts tests/phase-j-rhythms.test.ts tests/phase-e-academy.test.ts tests/phase-h-onboarding.test.ts tests/phase9-scout.test.ts tests/nav.test.ts` — 48 passed (2026-09-09). `npx tsc --noEmit` clean. Scout send unchanged. Phase L deferred. Phase M still owns production keys. Production Neon `production-launch` 2026-09-09: `0016` applied; `companies.gtm_tier` / `companies.gtm_region` present. `npm run db:check` OK (connected; vector + pg_trgm; 17 applied vs 13 expected tags through 0012; `developmentFixturesDetected=false`). | Production is already live; this is operating-complete GTM. `0013` was already on prod; `0014`–`0016` applied 2026-09-09. |
| L | Talent / jobs / applications list pagination | TESTED | Volume | Product Build | `listJobs` / `searchActiveCandidates` / `listApplications` | None | Server-side page + UI pager; hard max 100 | `80f7398` on `main` (2026-09-09). `lib/pagination.ts`; jobs/talent/applications/pipeline pagers. Recruiter still lacks `opportunities.read`. `npx vitest run tests/phase-l-scale.test.ts` plus related suites — 102 passed. `npx tsc --noEmit` clean. Production Neon `production-launch` 2026-09-09: `0017_quiet_scale` applied (was pending; 18 hashes vs 17 before). `npm run db:check` OK (connected; vector + pg_trgm; 18 applied vs 13 expected tags through 0012; `developmentFixturesDetected=false`). Checkpoint `prod-migrate-checkpoint-20260909-0017` expires 2026-09-16. Ops record `c6e505e`. | Default 25 / max 100. `0017` is on production-launch. |
| L | Command Center SQL rewrite | TESTED | Phase J rhythms | Product Build | `recruitingAnalytics` | None | Counts + 8-row exception lists; no full graph hydrate | `80f7398`. `lib/repositories/recruiting-delivery.ts` uses SQL `count()` / `groupBy` and `ANALYTICS_EXCEPTION_LIMIT = 8`. Same vitest/tsc run. | `/app` overview stays on counts. |
| L | SkillBridge alert-rule UI | TESTED | Existing `skillbridge_alert_rules` | Product Build | `/app/military/skillbridge/alerts` | None | Authorized roles can persist thresholds | `80f7398`. `skillbridge.manage` / `military.review`. Added `window_starting_soon` / `window_ending_soon` (default 14). No new matching rules. Production-launch 2026-09-09: both enum values present (`information_schema` / `pg_enum`). | 14-day windows are no longer constants. |
| L | ETS / EAOS aliases | TESTED | SkillBridge profile | Product Build | `skillbridge_profiles.ets_date` / `eaos_date` | None | Navy/USMC language stored and labeled | `80f7398`. Same Talent Network candidate. UI labels EOS / ETS / EAOS / separation / retirement. Production-launch 2026-09-09: `skillbridge_profiles.ets_date` / `eaos_date` present. | Do not invent MOS maps. |
| L | Scheduled report exports | TESTED | `reports.export` | Product Build | `report_export_schedules` / Inngest cron | None | Simple daily/weekly stored jobs | `80f7398`. `lib/reporting/schedules.ts`; cron `TZ=America/New_York 15 6 * * *`. Talent PII stays on-demand. Production-launch 2026-09-09: `report_export_schedules` / `report_export_jobs` present; org FKs `ON DELETE restrict`. | Not a BI platform (DEC-RPT-001). |
| L | Scout result cap | TESTED | Closed commands | Product Build | Scout DTO `offset` | None | 25 per page, hard max 100 | `80f7398`. `SCOUT_PAGE_SIZE` / `SCOUT_MAX_RESULTS`. Send gates unchanged. | No unbounded PII in chat. |
| L | Semantic embedding dimension (DEC-SEM-001) | IN REVIEW | Embedding model choice | Product Build | `vector(1536)` | Live `AI_MODEL_EMBEDDING` + known dimensions | Keep hash embeddings until a production model and dimension are chosen together | `80f7398`. `AI_MODEL_EMBEDDING` is not configured in this environment. Retrieval stays development-hash `vector(1536)`. No dimension migration. | Deferred dimension change. Do not change 1536 without a model. |
| L | BLS / Census live adapters | TESTED | DEC-WF-004 / DEC-INT-002 | Product Build | Integration Hub | API keys for live | Live when keys present; labeled fixtures when not | `80f7398`. `isBlsConfigured` / `isCensusConfigured`. No fake live. | Production keys remain Phase M. |
| L | Candidate self-scheduling | TESTED | CalendarProvider; Phase I tokens | Product Build | `/schedule/[token]` | None | Tokenized public slot pick; no `/app` session | `80f7398`. Rate-limited. `liveScheduling` only when Google/Microsoft refresh tokens exist. | Do not scrape Google. |
| L | Public hire `/onboarding/access` | TESTED | ATS hire onboarding | Product Build | `public_access_tokens` purpose `hire_onboarding` | None | Signed tokens issued on start onboarding; distinct from staff Academy | `80f7398`. `/onboarding/access` vs `/app/academy/onboarding`. New-hire tasks only. Production-launch 2026-09-09: `public_access_tokens` present; org FK `ON DELETE restrict`. | Not PierOne staff onboarding. |
| L | Thin applicant status (not a candidate portal) | TESTED | Existing applications | Product Build | `/careers/status/[token]` | None | Read-only stage/job from the same application row | `80f7398`. Recruiter issues a status link. No second candidate database. No public registration. | Later full candidate portal remains later product. |
| L | Client portal | DEFERRED | DEC-SEC-001 | Product | Post-launch backlog | Reconsider internal-first / invite-controlled | Tight share link only if authorized; no public registration | Required decision: DEC-SEC-001 reconsideration for a real client login. `80f7398` did not add public registration. | Did not half-build a SaaS portal. |
| L | Mobile UX (responsive, not native) | BUILT | Command Center / Jobs / Talent / Academy | Product Build | Existing layouts | None | Overflow and wrap fixes if broken | `80f7398`. MetricCard scales; TabNav already wraps; tables already `overflow-x-auto`; Academy table already scrolls. | Not a redesign. Browser verification not run this pass. |
| L | Advanced training / full LMS | DEFERRED | Phase E Academy | Product | Backlog | Full LMS decision | Academy stays lightweight; completion does not grant permissions | `80f7398` did not start a full LMS. | Do not auto-grant permissions. |
| Leftover | Proposal path obviousness (one primary CTA) | COMPLETE | Commercial spine already built (`internal_review` before send) | Product Build | Opportunities / proposals | None | One obvious path everywhere | `f28ad50`. `resolveCommercialPrimaryCta` + opportunity/discovery/solution/proposal/contract CTAs. Build proposal only after approved plan and no in-flight proposal. `npx tsc --noEmit` clean. `npx vitest run tests/leftover-ia.test.ts tests/nav.test.ts tests/phase-e-academy.test.ts tests/rbac.test.ts tests/phase3-permissions.test.ts` — 37 passed (2026-09-09). | One primary CTA per stage. No second proposal factory. |
| Leftover | AI costs IA under Admin → AI & Automation | COMPLETE | Server gate already `agents.manage` | Product Build | Nav / AI Operations | Destination IA | Costs not in Recruiter IA | `f28ad50`. Nav label Admin → AI & Automation. Costs/runs/prompts stay `agents.manage`. Review Queue stays `agents.read`. Recruiter still lacks both. Same vitest/tsc run (2026-09-09). | Gate shipped with `0f3fa1e`; destination IA finished this pass. |
| Leftover | Reports as operating questions | COMPLETE | Audit P0/P1 | Product Build | Reports UI | None | Titles/empty states are questions | `0f3fa1e` / `30f0dec` plus leftover copy cleanup in `f28ad50`. Removed “Stored records only” / “Stored jobs only” / “No stored records answer this yet.” Same vitest run. | Keep row; do not delete. |
| Leftover | Legal templates — honest attorney-approved labels | COMPLETE | Audit P0/P1 | Product Build | Legal package UI | None | “Attorney-approved” only if `attorney_approved` | `0f3fa1e`–`30f0dec` plus remaining “Structural templates…” copy replaced in `f28ad50` with DRAFT — NOT APPROVED FOR USE vs ATTORNEY APPROVED. Same vitest run. | Packages stay inside operational workflows. |
| Leftover | One Add Company path | COMPLETE | CRM IA | Product Build | Companies UI | None | Single create path; no duplicate dead-ends | `f28ad50`. Companies is the only create form. Command Center has no Add company. Scout CREATE does not add a company. Same vitest/tsc run (2026-09-09). | Primary CTA is Add company. |
| Leftover | Duplicate / dead-end IA | COMPLETE | Nav lifecycle work | Product Build | `nav-config.ts` | None | Old requisition/search URLs either redirect or stay intentionally | `f28ad50`. `/app/recruiting/requisitions` and `/app/search-projects` redirect to Jobs views. Search-project detail stays. Connected tools vs Integration Hub titles aligned. Same vitest run. | Jobs consolidation kept. |
| Leftover | Concept clarification (service names, access vs title) | COMPLETE | Phase C + D | Managing Partner | Service catalog vs public copy | DEC-SVC-005 display name | One name per offer; Title ≠ Access explained | DEC-SVC-005: display name **Professional & Technical Search**; slug `professional-search`. Academy `operating-concepts` plus in-page ConceptNotes. Title ≠ Access stays in Academy. | Operator copy + public copy aligned. No schema/workflow-key rename. |
| Leftover | Signed-in click-through of leftover IA CTAs | BLOCKED | Leftover IA CTAs | Product Build | Production `https://app.pieronepartners.com` | Signed-in Clerk session | Companies Add company; opportunity one-primary CTAs; proposal review/approve/send visibility; Admin → AI & Automation; Jobs redirects; Finance spine; Academy operating-concepts | 2026-09-09: cursor-ide-browser could not open a tab (`No browser tab available`). Production `/app/companies` and `/sign-in` without a session return the public splash, not the OS. No local Next session. Did not invent credentials. Public `https://pieronepartners.com/services/professional-technical-search` shows **Professional & Technical Search**. CTA code + `tests/leftover-ia.test.ts` reviewed; no CTA defect found to fix. | Operator must click through once a Clerk session exists. |
| Leftover | Finance continuity visibility | COMPLETE | Proposal / contract / invoice / AR tables exist | Ops / Finance | Finance screens | None | Operator path is one spine; QB live post exists in code when tokens are set | `f28ad50`. FinanceSpine on Finance, invoices, AR, payments, revenue, contracts, projects, proposal pricing. DEC-FIN-002. Same vitest run. Production QB still BLOCKED on `QUICKBOOKS_*`. | Not a GL. |
| Parked | Phase 11 / research runtime `1a356d3` | DEFERRED | Explicit human review | Product Build | `follow-on/parked-working-tree` | Later AI module decision | Not merged | Commit `1a356d3` parked. | Do not start. |
